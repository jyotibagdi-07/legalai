<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>LegalAI — Templates</title>
  <style>
    :root{
      --blue:#1f66b3;
      --navy:#123663;
      --muted:#f4f6f8;
      --card:#ffffff;
      --text:#233043;
      --accent:#0b5fb8;
      --radius:10px;
      --shadow: 0 6px 20px rgba(16,30,50,0.08);
      font-family: Arial, sans-serif;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      background:#f7f9fc;
      color:var(--text);
      line-height:1.4;
    }

    /* Top bar */
    .topbar{
      height:60px;
      background:var(--blue);
      color:#fff;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:0 24px;
    }
    .topbar .brand{font-weight:700;font-size:18px;}
    .navlinks a{color:#fff;text-decoration:none;margin-left:20px;}
    .navlinks a:hover{text-decoration:underline;}

    /* Layout */
    .container{
      display:grid;
      grid-template-columns: 400px 1fr;
      gap:20px;
      padding:20px;
    }

    /* Left form card */
    .card{
      background:var(--card);
      border-radius:var(--radius);
      padding:20px;
      box-shadow:var(--shadow);
    }
    .card h2{margin-top:0;font-size:18px}
    .field{margin-bottom:12px;display:flex;flex-direction:column;}
    .field label{font-size:14px;margin-bottom:4px;}
    .field input,.field select,.field textarea{
      padding:10px;
      border:1px solid #ccc;
      border-radius:6px;
      font-size:14px;
    }
    .btn{
      background:var(--accent);
      color:#fff;
      border:none;
      padding:10px 16px;
      border-radius:6px;
      cursor:pointer;
      font-weight:600;
    }
    .btn:hover{background:#084d91}

    /* Right: templates grid */
    .templates-panel{display:flex;flex-direction:column;gap:16px;}
    .templates-grid{
      display:grid;
      grid-template-columns: repeat(auto-fill,minmax(200px,1fr));
      gap:12px;
    }
    .template-card{
      background:#fff;
      padding:12px;
      border-radius:8px;
      box-shadow:0 2px 6px rgba(0,0,0,0.1);
      cursor:pointer;
      transition:transform 0.2s;
    }
    .template-card:hover{transform:translateY(-4px);}
    .tmpl-title{font-weight:600;margin-bottom:6px;}
    .tmpl-desc{font-size:13px;color:#555;}
    .preview{
      min-height:150px;
      background:#fff;
      padding:12px;
      border-radius:8px;
      border:1px solid #ddd;
    }

    @media(max-width:900px){
      .container{grid-template-columns:1fr;}
    }
  </style>
</head>
<body>
  <header class="topbar">
    <div class="brand">⚖️ LegalAIAssistant</div>
    <nav class="navlinks">
      <a href="index.html">Home</a>
      <a href="templates.html">Templates</a>
    </nav>
  </header>

  <main class="container">
    <!-- Left side -->
    <section class="card">
      <h2>Generate Rent Agreement</h2>
      <div class="field">
        <label>Tenant Name</label>
        <input type="text" id="tenant" value="Alice Smith">
      </div>
      <div class="field">
        <label>Landlord Name</label>
        <input type="text" id="landlord" value="Acme Properties Inc.">
      </div>
      <div class="field">
        <label>Property Address</label>
        <textarea id="address">B-108 b block</textarea>
      </div>
      <div class="field">
        <label>Monthly Rent Amount</label>

